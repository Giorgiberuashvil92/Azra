# Railway will provide PORT automatically.

# Optional for the current frontend proxy routes.
# Point this to your backend API when it is deployed.
AZLA_API_URL=https://your-api-service.up.railway.app/api

# Add this from Railway PostgreSQL using a reference variable:
# DATABASE_URL=${{Postgres.DATABASE_URL}}
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE
